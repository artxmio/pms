<Window 
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    x:Class="ProjectManagementStudio.View.MainWindow.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ProjectManagementStudio.ViewModel.MainWindow;assembly=ProjectManagementStudio.ViewModel"
    mc:Ignorable="d"
    Title="MainWindow" Height="900" Width="1600"
    ResizeMode="NoResize"
    WindowStyle="None"
    WindowState="Maximized"  
    d:DataContext="{d:DesignInstance vm:MainWindowViewModel}">

    <Window.Resources>

        <!-- // Стиль для кнопки закрытия приложения //-->
        <Style x:Key="Close_Button" TargetType="{x:Type Button}">
            
            <Setter Property="Background" Value="#3D5754"/>
            
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Red"/>
                </Trigger>
            </Style.Triggers>
            
        </Style>

    </Window.Resources>

    <Window.Background>
        <ImageBrush ImageSource="{DynamicResource Background}"/>
    </Window.Background>
    
    <d:Window.Background>
        <d:ImageBrush ImageSource="../Source/DefaultTheme/backgroundImage.jpg"/>
    </d:Window.Background>

    <Grid>
        <!-- // Фрейм для отображения страниц регистрации и авторизации //-->
        <Frame Content="{Binding ActivePage}" NavigationUIVisibility="Hidden"/>

        <Button Command="{Binding CloseCommand}"
            BorderBrush="Transparent"
            Margin="0,20,20,0" 
            Width="30" 
            Height="30" 
            VerticalAlignment="Top" 
            HorizontalAlignment="Right" 
            Background="Transparent" 
            Style="{StaticResource ResourceKey=Close_Button}">

            <Image x:Name="close" d:Source="../Source/DefaultTheme/Close_icon.png"
                   Source="{DynamicResource CloseButton}">
                <Image.Style>
                    <Style TargetType="Image">
                        <Setter Property="Opacity" Value="0.5"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="1"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
            </Image>
            
        </Button>
        
    </Grid>
</Window>
